# rocareer/webman-migration — Phinx 迁移工具插件：数据库迁移管理

> 面向用户 / 后台助手的产品级知识。开发者文档见 README / CHANGELOG / docs。

## 是什么

rocareer/webman-migration 是运行在 Webman 应用上的**数据库迁移（Migration）管理插件**。它把业界成熟的迁移能力封装为 Webman 插件，让项目与全家桶各插件**一键执行数据库结构变更**——建表、加字段、加索引、改表结构，均通过可版本化的迁移脚本完成。

它把「项目自身迁移」与「全家桶各插件自带迁移」集中管理，一条命令跑完、无需逐包登记；安装任意 rocareer 插件后其迁移自动纳入。

## 用途与意义

数据库结构随业务持续演进，手工改表难留痕、难追踪，也无法让各环境保持一致，结构变更更难版本化回滚。迁移工具把每次结构变更写成随代码入库的脚本，按固定顺序执行，保证任何环境都能一致升级到目标结构。它同时解决全家桶各插件各自携带数据结构的痛点，宿主不必手动登记各包迁移；并统一表前缀，让迁移建表与框架 ORM 约定一致，避免表名冲突。

## 主要功能

- **一键执行全部迁移**：一条命令跑完项目与全部插件的待执行迁移。
- **自动发现插件迁移**：新装全家桶插件后迁移自动纳入，无需改配置。
- **统一表前缀**：迁移建表与 ORM 同前缀，迁移记录表同步加前缀，避免命名冲突。
- **包级迁移基类**：为开发者提供统一基类，自动处理表前缀等约定。
- **幂等安全**：迁移可重复执行，已有结构则跳过，重跑不报错、不重复建表。
- **可指定配置与目标版本**：支持自定义配置文件、只升级到指定版本。

## 关键概念

- **数据库迁移**：一次结构变更写成的可版本化脚本，随代码管理。
- **迁移来源**：来自「项目自身」与「各插件携带」两部分，插件安装即自动纳入。
- **表前缀**：表名前统一加的前缀，用于多应用共存不冲突，与 ORM 一致。
- **迁移记录表**：记录已执行的迁移，据此只执行新迁移的增量管理。
- **幂等**：脚本重复执行安全，结构已存在则跳过，可反复运行与回退。

## 使用入口

本插件为命令行/运维入口，无需后台菜单。在 Webman 项目根目录执行 `php webman migrate:run` 跑全部迁移；追加 `--config <配置文件>` 指定配置、`--target <版本号>` 只升到指定版本。安装即可用；卸载仅移除配置，已执行的迁移与已建表不删除，数据归项目所有。
